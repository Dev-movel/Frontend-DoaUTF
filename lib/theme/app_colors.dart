import 'package:flutter/material.dart';

class AppColors {
  // =========================
  // Superfícies
  // =========================
  static const background = Color(0xFFFAFAF5);
  static const surface = Color(0xFFFFFFFF);

  static const surfaceContainerLow = Color(0xFFF4F4EF);
  static const surfaceContainer = Color(0xFFEEEEE9);

  // nomes originais
  static const containerHigh = Color(0xFFE8E8E3);
  static const containerHighest = Color(0xFFE3E3DE);

  // aliases Material 3 / compatibilidade
  static const surfaceContainerHigh = containerHigh;
  static const surfaceContainerHighest = containerHighest;
  static const surfaceContainerLowest = surface;

  // =========================
  // Texto / conteúdo
  // =========================
  static const onBackground = Color(0xFF1A1C19);
  static const onSurface = Color(0xFF1A1C19);
  static const onSurfaceVariant = Color(0xFF40493D);

  static const outline = Color(0xFF707A6C);
  static const outlineVariant = Color(0xFFBFCABA);

  // =========================
  // Primária (verde)
  // =========================
  static const primary = Color(0xFF0D631B);
  static const primaryContainer = Color(0xFF2E7D32);

  static const primaryFixed = Color(0xFFA3F69C);
  static const primaryFixedDim = Color(0xFF88D982);

  static const onPrimary = Color(0xFFFFFFFF);
  static const onPrimaryContainer = Color(0xFFCBFFC2);

  static const inversePrimary = Color(0xFF88D982);

  // =========================
  // Secundária (azul)
  // =========================
  static const secondary = Color(0xFF00629E);
  static const secondaryContainer = Color(0xFF62B4FE);

  static const secondaryFixed = Color(0xFFCFE5FF);
  static const secondaryFixedDim = Color(0xFF99CBFF);

  static const onSecondary = Color(0xFFFFFFFF);
  static const onSecondaryContainer = Color(0xFF004470);

  // =========================
  // Terciária (laranja)
  // =========================
  static const tertiary = Color(0xFF824600);
  static const tertiaryContainer = Color(0xFFA55B00);

  static const onTertiary = Color(0xFFFFFFFF);
  static const onTertiaryContainer = Color(0xFFFFEEE3);

  // =========================
  // Erro
  // =========================
  static const error = Color(0xFFBA1A1A);
  static const errorContainer = Color(0xFFFFDAD6);

  static const onError = Color(0xFFFFFFFF);
  static const onErrorContainer = Color(0xFF93000A);

  // =========================
  // Inverso
  // =========================
  static const inverseSurface = Color(0xFF2F312E);
  static const inverseOnSurface = Color(0xFFF1F1EC);
}